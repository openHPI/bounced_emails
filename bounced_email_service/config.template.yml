---
develop:
  handler:
    dbfile: {bounced_emails_path}/bounced_emails.db
    permanent_bounced_emails_path: {bounced_emails_path}/bounced_emails
    temporary_threshold: 3
    domains:
      mydomain.de:
        base_url: http://localhost:7001/emails/{address}/suspend
